apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- gotk-sync.yaml

# The Flux controller CRDs/deployments are generated by `flux bootstrap`.
# They are intentionally omitted from this curated public snapshot to avoid
# carrying a large generated manifest in the portfolio repo.
